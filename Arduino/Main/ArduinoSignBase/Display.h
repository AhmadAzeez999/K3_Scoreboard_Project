#include <Adafruit_NeoPixel.h>
#include "Base.h"

namespace display
{
  void displayPixel(int row, int col); // For displaying a single pixel
  void displayA(); // This is a temporary function and for testing only
}